#include "observador.h"

bool Observador::agregado_edificio(std::pair<int, int>
posicion_central, ObjetoDune* objeto) {
	return true;
}

/*void Observador::agregar_edificio(ObjetoDune* objeto, 
int id_objeto, std::pair<int, int> centro) {}*/

bool Observador::edificio(std::pair<int, int>
posicion_central, ObjetoDune* objeto) {
	return true;
}